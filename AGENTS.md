# Cashier Server Rust

## Tips

- use `rg` (ripgrep) for fast text search across files
- use `serena` to navigate through code
- use `uv run python` for running Python scripts

## Projects

The project plans are kept in the `/doc` directory.
